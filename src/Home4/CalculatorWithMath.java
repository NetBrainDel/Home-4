package com.company;

import java.math.MathContext;
import java.lang.Math;
import java.lang.Object;
public class CalculatorWithMath extends CalculatorWithOperator{

        double a = 0;
        double b = 0;

        // деления, умножение, вычитание, сложение\\
        double div = a / b;
        double mul = a * b;
        double sub = a - b;
        double sum = a + b;

        // Возведение в степень, Модуль числа, Корень из числа\\
        double step = Math.pow(a,b);
        double abs = Math.abs(a);
        double kor = Math.sqrt(a);









}
